# 工作簿处理示例

这个示例展示了一条紧凑的工作簿处理流程：

- 创建工作簿
- 写入业务数据
- 计算公式
- 保存为 `.xlsx`

## 重点看什么

- `SheetRuntime` 和 `CreateSession()` 是运行时入口
- `CreateWorkbook("Orders")` 创建当前处理流程使用的工作簿
- 通过熟悉的 A1 地址写入单元格值和公式
- `Calculate()` 在输出前刷新公式结果
- `Save()` 生成最终工作簿字节包

## 适合的场景

这类模式适合报表生成、模板填充和自动化工作簿导出任务。
