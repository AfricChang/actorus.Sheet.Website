# 服务端工作流示例

这个示例从客户端视角展示服务端处理链路。

## 流程

- 把工作簿内容发送到托管计算接口
- 保存返回的工作簿包
- 再次打开它，并启用重新计算
- 从返回的工作簿契约里读取最终公式结果

## 适合的场景

这类模式适合后端 API、内部流程服务、队列任务以及其他托管式工作簿处理链路。

## 前提

需要先启动兼容的 `AcTorus.Sheet` service host，再把它的 base URL 传给这个示例。
